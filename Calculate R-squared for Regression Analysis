import numpy as np

def r_squared(y_true, y_pred):
	sum_of_squared_residuals = np.sum(np.square(y_true - y_pred))
	mean = np.mean(y_true)
	sum_of_squares = np.sum(np.square(y_true - mean))
	R = 1 - (sum_of_squared_residuals/sum_of_squares )
	return R
	
